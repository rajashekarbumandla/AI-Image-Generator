# AI-Powered Text-to-Image Generator 

This project implements an **AI-powered text-to-image generator** using open-source Stable Diffusion models.  


Users can:
- Enter text prompts
- Choose visual style (photorealistic / artistic / cartoon)
- Adjust generation parameters
- Generate multiple images per prompt
- Download images in PNG/JPEG formats
- View metadata for each generation

All images include a watermark indicating AI-origin and simple safety filters are applied.

---

## 🎯 Features

- ✅ Open-source **Stable Diffusion** (via Hugging Face `diffusers`)
- ✅ Local execution with **GPU support** (CPU fallback)
- ✅ Streamlit web UI:
  - Text prompt input
  - Negative prompt (optional)
  - Style presets
  - Controls for:
    - Number of images
    - Inference steps
    - Guidance scale
- ✅ Images saved with metadata (prompt, style, timestamp, parameters)
- ✅ Export to **PNG** and **JPEG**
- ✅ Simple unsafe prompt filtering
- ✅ Watermark: `"AI-generated by Talrn-ImageGen"`

---

## 🏗️ Architecture

```text
User (Browser)
    ↓
Streamlit Web UI (`app.py`)
    ↓
Stable Diffusion Pipeline (`models.py`, `diffusers`, `torch`)
    ↓
Image Generation (`generator.py`)
    ↓
Images + Metadata stored in `outputs/`


## 🧠 Tech Stack

| Category | Technology |
|----------|------------|
| Language | Python 3.10+ |
| ML Framework | PyTorch (CPU or CUDA GPU acceleration) |
| Model | Stable Diffusion v1.5 (`runwayml/stable-diffusion-v1-5`) |
| Core Libraries | diffusers, transformers, accelerate, safetensors |
| UI Framework | Streamlit |
| Image Processing | Pillow |
| Metadata Handling | JSON + OS filesystem |

## ⚠️ Limitations

- 🐢 **Slow performance on CPU**  
  Stable Diffusion is computation-heavy, so CPU inference may take **1–3+ minutes** per image depending on settings.

- 🎭 **Generation artifacts may occur**, such as:
  - Distorted hands or faces  
  - Unnatural anatomy or missing details  
  - Style inconsistencies with complex prompts  

- 🛑 **Safety filter is keyword-based only**  
  It does not implement advanced AI moderation.

- 💾 **High memory usage**  
  Requires at least **8GB RAM** for smooth execution (GPU recommended for real-time use).

- 🎨 **Limited model flexibility**  
  Currently supports only SD v1.5; no model switching from UI yet.


-🛑 Safety filter is keyword-based only, not a full AI content moderation system.

-🎨 Limited range compared to larger/finetuned models (e.g., SDXL, custom LoRA models).

-💾 Higher memory usage on low-end machines (8 GB RAM recommended).
